on:
  push:
jobs:
  install:
    runs-on: ubuntu-latest
    container: 'node:14'
    steps:
      - uses: actions/checkout@v2
      - run: npm ci
  build:
    runs-on: ubuntu-latest
    container: 'node:14'
    steps:
      - run: npm run build
    needs: install
